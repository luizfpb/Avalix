# BodyTrack — DECISIONS.md
Cole este arquivo no início de chats novos sobre o projeto. Última atualização: v2.0 (jul/2026). App = Avalix (repo/pacote/Supabase internos seguem "bodytrack"). Módulo de treino completo (A–K) + melhorias L–T entregues. Migrations 0001–0019 no repo; 0001–0018 APLICADAS (types regerados); a 0019 precisa ser APLICADA e os types regerados ANTES de subir o código da v2.0 (mesma regra da 0013/0016/0017/0018 — get_anamnese_intake mudou o retorno e o app passou a chamar save_assessment/create_workout_log). Consentimento LGPD em 1.0. 2FA FECHADO (jul/2026): TOTP habilitado no painel e testado ponta a ponta EM PRODUÇÃO pelo Luiz (enrollment com Authy + login com desafio + dados carregando sob aal2); scripts/test-2fa.mjs fica como regressão. Lembrete operacional: perder o autenticador não tem autoatendimento — remover o fator exige o painel (Authentication > Users > usuário > fatores) ou service role; o Authy tem backup multi-dispositivo, manter ligado.

## O que é
PWA de avaliação física e postural para profissionais (avaliadores, personais, nutricionistas, academias). V1 para beta com uma avaliadora real. Dev: Luiz, solo, Windows + VSCode + CMD. Sem loja de app, sem domínio próprio por ora.

## Stack (fechada)
- Front: Vite + React + TypeScript, Tailwind + shadcn/ui, Recharts, TanStack Query, react-hook-form + zod, react-router.
- Back: Supabase free tier (Auth com confirmação de e-mail + MFA TOTP, Postgres com RLS, Storage privado). Sem backend próprio, sem Python.
- Deploy: Cloudflare Pages (domínio *.pages.dev no beta).
- PDF client-side com @react-pdf/renderer; CSV com papaparse; testes com vitest.

## Decisões-chave
- Tudo modelado como organização (multi-tenant) desde a migration 0001; V1 opera solo, UI de equipe depois. Termo exibido configurável: aluno/cliente/paciente/atleta/avaliado.
- Camadas: RLS = visibilidade; triggers = integridade (org herdada do pai, paths canônicos, relações congeladas, avaliador legítimo); constraints = domínio. org_id denormalizado nas filhas, copiado por trigger b1 e congelado depois. Triggers before insert disparam em ordem alfabética, daí os prefixos b1/b2 nos nomes.
- Visibilidade central em `app.can_view_subject`: owner/admin veem tudo; avaliador comum vê os seus, ou todos se `evaluators_see_all`. Filhas herdam via `can_view_*_id`.
- Consentimento é pré-condição de banco, não de UI: `app.has_active_consent*` no with check de INSERT de assessments, leituras, posture_sessions, posture_photos e posture_annotations. Revogação (`revoked_at`) bloqueia coleta nova; select e correção de dado antigo continuam; apagar é só pela exclusão definitiva. Registro de consentimento imutável exceto revoked_at.
- Anamnese respondida pelo próprio aluno por LINK (migration 0017): capability token de 256 bits, uso único, expira em 7 dias, guardado só como hash sha256 no banco (distinto de "path difícil de adivinhar" da 0002 — aqui o token É o segredo, como link de redefinir senha; vazamento do banco revela só o hash). Acesso anônimo por DUAS RPCs security definer (`get_anamnese_intake`/`submit_anamnese_intake`) que validam o token por dentro; nenhuma policy aberta pra `anon`. A resposta entra como intake `submitted` (pendente) e só vira anamnese oficial no aceite do personal via `accept_anamnese_intake` (security invoker, transação: cria consentimento + anamnese; gate recalculado no TS e passado por parâmetro). LGPD reforçada: o próprio titular consente (nome/versão/hash/timestamp), mais forte que o personal marcar no lugar dele. Pendência bem visível pro personal (badge, dashboard, perfil) pra não cair em esquecimento.
- `evaluator_id` not null com default auth.uid() em subjects, assessments e posture_sessions; trigger `check_evaluator`: atribuído precisa ser membro da org; owner/admin atribuem a qualquer membro, avaliador comum só a si; valida no insert e só quando o campo muda no update. subjects usa on delete restrict: excluir conta exige excluir a org (ou reatribuir) antes.
- Fotos: cliente nunca manda path. Insert em posture_photos dispara trigger que gera `{org}/{subject}/{session}/{photo_id}.{ext}` e `_thumb` (ext por `format`: webp/jpeg) e devolve no select da inserção. Fluxo: linha primeiro, upload depois exatamente nesses paths; falhou o upload, apaga a linha. Storage de fotos não valida por pasta: a policy resolve o name para a linha de posture_photos (paths únicos/indexados) e aplica can_view_photo_id; upload exige também consentimento vigente. Path não registrado = negado. Buckets privados, URL assinada TTL 300 s, mime types e tamanho limitados no bucket.
- Protocolos são código TS (registry tipado + função pura + testes com vetores publicados), não tabelas. Resultados em snapshot jsonb em assessments.results com engine_version. V1: JP7, JP3, JP-Ward (mulheres), Durnin & Womersley, US Navy; conversões Siri e Brozek. Petroski/Guedes/Faulkner/Sloan fora até verificação. Nenhuma fórmula sem teste batendo com exemplo publicado.
- Dobras em mm, 1 a 3 aferições por ponto com média; circunferências em cm com extras e customizadas; sexo M/F (exigência das equações).
- Postural V1: captura/upload, categorias, compressão client-side (canvas ~1600 px, webp com fallback jpeg, remove EXIF/GPS), thumbs 320 px, comparação lado a lado/grade/overlay por opacidade. Anotações (v1.5): editor de pontos/linhas/ângulos sobre a foto (componente AnnotationCanvas), coordenadas normalizadas em 0..1 (alinham em qualquer tela), geometria pura e testada (ângulo/inclinação medidos em espaço com proporção correta), arrastar/apagar/limpar, salvas no payload jsonb de posture_annotations (um registro por foto, insert exige consentimento vigente pela RLS). Overlay read-only no lightbox, na grade (badge "anotada") e na tela de comparação lado a lado (toggle "Anotações"), onde dá pra ver o mesmo ângulo nas duas fotos.
- PDFs não armazenados (regenerados do snapshot). CSV default internacional + opção Excel BR, UTF-8 com BOM. Resumo IA identificado/anônimo. Exportações logadas em audit_logs (EXPORT_CSV/EXPORT_JSON/PDF_REPORT/AI_SUMMARY).
- LGPD: dados sensíveis (art. 11); consentimento eletrônico no app (texto versionado + hash, nome digitado, titular ou responsável p/ menor de 18); exclusão definitiva remove arquivos do storage ANTES do banco (a policy resolve o objeto pela linha). Profissional = controlador, BodyTrack = operador.
- Auditoria mínima (sem payload). Custo: free tier; com 50 clientes, fotos migram para R2 via costura única em lib/storage.ts (a regra de visibilidade por foto será reimplementada no Worker), total projetado R$0-10/mês. Supabase Pro fora de cogitação.
- Operação: GitHub Actions com keep-alive (a cada 3 dias) e backup semanal pg_dump criptografado (gpg) como artifact; restore testado antes do beta.

## Convenções
- Tabelas/colunas/código em inglês; UI em pt-BR. Comentários escassos, em pt-BR, explicando porquês. Commits curtos em pt-BR, sem emoji.
- Migrations numeradas em supabase/migrations/. Testes colocalizados (x.test.ts ao lado de x.ts). Features em src/features/, infra em src/lib/.
- Fluxo no chat: colar erro (não arquivo inteiro), pedir diff para mudanças pequenas, "ok" quando comando funcionar, agrupar dúvidas.

## Estado atual
Etapa 3.2 entregue e validada em runtime: auth Supabase (cadastro/login/logout/recuperação), AuthProvider com onAuthStateChange, OrganizationProvider via TanStack Query lendo org_members, roteamento centralizado (resolveRedirect + RouteGuard), onboarding criando org via RPC create_organization(p_name) e gravando subject_term por update, dashboard e páginas placeholder. Correção de boot: o cliente lia VITE_SUPABASE_ANON_KEY (inexistente) e quebrava; passou a usar VITE_SUPABASE_PUBLISHABLE_KEY, a convenção do projeto (o bug afetava também produção no Cloudflare). Senha mínima 6 (padrão Supabase) adotada como canônica. Tabela de membros é org_members; database.types.ts em src/lib/. Formulários de auth em estado nativo. MFA adiada. Entregue na sequência: Avaliados + Consentimento (CRUD de subjects com subject_term na UI, react-hook-form + zod, responsável obrigatório p/ menor de 18; consentimento eletrônico versionado com hash sha256 e revogação) e Avaliação física (motor de protocolos puro e testado em src/features/assessment/protocols — JP7, JP3, JP-Ward, Durnin-Womersley, US Navy + Siri/Brozek; registry tipado, ENGINE_VERSION, filtro por sexo; formulário com cálculo ao vivo gravando snapshot reproduzível em assessments.results + leituras). Avaliação postural (core): sessões + captura/upload de fotos com compressão client-side (canvas 1600px, webp com fallback jpeg, remove EXIF/GPS e corrige orientação), thumbs 320px, fluxo do DECISIONS (insere a linha, trigger gera os paths, sobe os arquivos, desfaz se falhar), exibição em grade com URLs assinadas (TTL 300s) e exclusão definitiva (arquivo antes da linha). Comparação postural (lado a lado e sobreposição com opacidade). Relatórios: PDF da avaliação via @react-pdf carregado sob demanda (chunk separado, fora do bundle principal); exportação CSV do histórico em dois dialetos (intl e Excel BR) com BOM UTF-8; exportações registradas em audit_logs. MFA TOTP: ativação na tela de conta (QR + segredo), desafio no login, guarda ciente de AAL (resolveRedirect + RouteGuard, AuthProvider rastreia o AAL da sessão, rota /mfa). Operação: GitHub Actions de keep-alive (a cada 3 dias, GET na REST API) e backup pg_dump criptografado com gpg (semanal, artifact com retenção 90 dias). v1.2 (alto valor/baixo esforço, do V1.1.md): IMC por avaliação calculado das colunas peso/altura (existe mesmo sem protocolo) com classificação pelas faixas da OMS (src/features/assessment/bmi.ts, testado); gráfico de evolução de % gordura, IMC e peso no detalhe do avaliado (Recharts, src/features/assessment/EvolutionChart.tsx), carregado sob demanda via React.lazy num chunk separado (Recharts fora do bundle inicial — endereça o P2 de code-split); error boundary global (src/components/ErrorBoundary.tsx) no main.tsx. v1.3: exclusão definitiva do avaliado (LGPD, direito de eliminação) — deleteSubjectCompletely remove os arquivos do Storage ANTES das linhas e o FK on delete cascade leva avaliações/leituras/sessões/fotos/consentimentos; UI é zona de perigo no editar com confirmação por nome digitado. MFA no banco (migration 0003, P0): app.mfa_satisfied() exige aal2 no JWT só quando o usuário tem fator MFA verificado (conta sem 2FA inalterada), adicionado a can_view_subject (cobre leitura de todo dado sensível + Storage) e aos with check de subjects; is_member/role_in ficam sem checar aal pra o shell carregar e rotear até /mfa. Classificação de % de gordura por faixas ACE por sexo (src/features/assessment/bodyFat.ts, testado) no detalhe da avaliação. Exclusão de sessão postural inteira (deleteSession, cobre sessões vazias). PWA real: vite-plugin-pwa (registerType autoUpdate), manifest + ícones (gerados por scripts/generate-pwa-icons.mjs), service worker que pré-cacheia só o shell estático e NÃO cacheia o Supabase (dados sensíveis/URLs assinadas); chunk do PDF fora do precache. v1.4 (UX/UI): cor de marca indigo como acento único sobre superfícies neutras (token --primary no index.css); AppShell com header sticky, marca e barra de navegação inferior fixa no mobile (cara de app) com ícones lucide; AuthLayout/Onboarding com marca e card; Dashboard real (stats de avaliados + atalhos, sem mais cards "em breve" falsos); lista de Avaliados com avatar de iniciais, busca e estados de carregando/vazio; componente Badge e helper initials (testado); code-split de todas as rotas via React.lazy (bundle inicial 706->489 KB); ícones do PWA e favicon regerados na cor da marca. keep-alive/backup blindados: secrets via env com erro claro se faltarem (a causa do exit code 3 era secret vazio). v1.5: anotações posturais (pontos/linhas/ângulos sobre a foto) — editor em /avaliados/:id/postural/:sessionId/foto/:photoId, AnnotationCanvas reusado em modo read-only no lightbox, geometria testada, sem migration (a tabela posture_annotations já existia). v1.6: campo "Medicamentos em uso" na avaliação (migration 0004 adiciona assessments.medications; destacado em caixa âmbar no formulário e no detalhe, e no PDF, separado das observações). Contraste: fundo da página passou a um cinza claro (--background) com cards/inputs brancos (input agora bg-card) e bordas mais visíveis, pra deixar claro onde se clica/digita. Deploy: public/_redirects (fallback SPA pro Cloudflare Pages não dar 404 ao recarregar uma rota). v1.7 (identidade visual + dark mode): marca com fonte Black Ops One usada SÓ no logo (wordmark BODYTRACK e reduzido B), nunca em texto de UI; os glifos são gerados em PATH por scripts/generate-brand-assets.mjs (opentype.js lendo o .woff do @fontsource) pra src/brand/*.svg — sem dependência da fonte em runtime. Componentes BrandLogo/BrandMark tingem o SVG via CSS mask (currentColor), legíveis em qualquer fundo. Favicon/ícones PWA: B #ECE3FA sobre quadrado #2A0E52 (scripts/generate-pwa-icons.mjs rasteriza o glifo com antialias; gera .png, .ico e .svg). Tema: dark é padrão; tokens shadcn completos pra :root (claro) e .dark (escuro) em src/index.css a partir das âncoras roxas (#130A20/#1C1030/#8B5CF6 etc.), magenta #D4537E como acento pontual (chart-2), semânticas legíveis (--success/--warning/--destructive-foreground). ThemeProvider (src/features/theme) aplica .dark no <html>, lê localStorage (só preferência de tema) e cai pra prefers-color-scheme; toggle claro/escuro/sistema em Configurações; script anti-flash no index.html (default dark). Auth/Onboarding usam o campo da marca (#2A0E52) com o logo claro; PDF usa plaqueta roxa com wordmark claro. Sem mexer em migrations/RLS/Storage/consentimento/workflows. v1.8: app renomeado para **Avalix** (AvalixFit é o domínio/nome completo do PWA). Logo e ícones regerados na mesma fonte (Black Ops One): wordmark AVALIX (src/brand/logo-avalix.svg + paths.ts) e marca letra A (mark-a.svg, favicon e ícones PWA). Textos atualizados: title, manifest (name AvalixFit / short_name Avalix), Onboarding, rótulo do 2FA (AvalixFit), plaqueta e rodapé do PDF (Avalix) e texto de consentimento (operador "Avalix", versão 0.2-rascunho). Repo/pacote/Supabase project_id continuam "bodytrack" (internos). Designs originais do Luiz (com glow/duas cores) podem substituir os SVGs vetoriais quando os arquivos forem adicionados. v1.9: anamnese e triagem de prontidão (docs/bodytrack_anamnese_spec.md). Migration 0005 cria a tabela anamneses no MESMO padrão das filhas (org herdado do subject, evaluator validado, freeze, auditoria, RLS via can_view_subject — logo já com gate MFA da 0003 — e INSERT exigindo consentimento vigente). Versionada por data (não sobrescreve). Conteúdo completo em payload jsonb; saídas do gate (liberado/nivel_encaminhamento/flag_encaminhamento) em colunas. Gate é módulo puro e testado (src/features/anamnesis/gate.ts): Camada A = PAR-Q+ (7 itens; qualquer Sim tira liberação) + matriz ACSM (sintomas/doença CMR/ativo → liberado/antes_vigorosa/antes_iniciar); red flags de coluna e gestação levantam flag de encaminhamento. UI: formulário AnamneseNova com condicionais (B6 só feminino, "se Sim→especificar"), listas repetíveis (cirurgias/medicamentos/dor) e resultado do gate ao vivo; detalhe read-only; seção no perfil do aluno com badge Liberado/Encaminhamento. Texto deixa explícito que é triagem inspirada em PAR-Q+/ACSM (redação própria), não diagnóstico. database.types.ts editado à mão (sem gen). FALTA aplicar a migration 0005 no Supabase. v1.10: catálogo rico de circunferências na avaliação (CIRCUMFERENCE_CATALOG em sites.ts) — sempre visível, todas opcionais, com bilateral D/E, braço relaxado e contraído, e lista de customizadas (is_custom). Salva todas as preenchidas em circumference_readings (não só as do protocolo), pra acompanhar a evolução; o protocolo continua lendo as que precisa do mesmo conjunto. Sem migration (a tabela já tinha site livre + is_custom). circumferenceLabel() traduz as chaves no detalhe. v1.11: visualização de dados. Geometria de gráficos pura e testada (src/features/reports/charts.ts: donutSlices, linePath) reusada no PDF. App: página /avaliados/:id/evolucao (Recharts) com rosca de composição (massa magra/gorda) + % gordura no centro, stats atuais com classificação ACE/OMS, linhas de evolução (% gordura/IMC, peso) e área de massas, barras horizontais de circunferências atuais e linhas de evolução dos perímetros mais medidos; link "Ver evolução e gráficos" no perfil. PDF (@react-pdf via primitivas Svg): rosca de composição + legenda + classificação + minigráfico de evolução da % de gordura (recebe history opcional). listSubjectCircumferences junta as circunferências por avaliação pra montar as séries. Sem migration. v1.14: módulo de montagem de treino para educadores físicos. Migrations 0006 (schema) / 0007 (RLS) / 0008 (seed de 275 exercícios globais), validadas executando num Postgres descartável antes de aplicar. Biblioteca em tabela única `exercises` com org_id NULLABLE (NULL = catálogo global curado, legível por todo autenticado e imutável pra usuário — sem policy de escrita que case org_id null; setado = custom da org com RLS padrão `org_id is null or is_member`). Todo exercício tem primary_muscle/secondary_muscles[]/equipment/movement_pattern, vocabulário travado por CHECK espelhando a taxonomia TS de src/features/workout/volume (20 grupos musculares, 14 equipamentos, 11 padrões). Plano em mesociclo: workout_plans (FK subject; snapshot de volume jsonb + volume_engine_version; FKs nullable source_assessment_id/source_posture_session_id como costura aberta avaliação→prescrição) → workout_days (divisões A/B/C) → workout_exercises (ordenados, séries/reps/RIR/descanso) + workout_week_overrides (override manual por semana, sparse) + workout_weeks (deload/rótulo). Mesmo padrão das filhas (org herdado por trigger b1; b2 valida que o exercício é global ou da mesma org e que as fontes são do mesmo subject; freeze das colunas relacionais; auditoria só no plano). SEM gate de consentimento: plano é saída profissional, não coleta de dado sensível do titular. Motor de volume puro e testado: contagem fracionada primário 1.0 / secundário 0.5 (método 'fractional', de melhor evidência nas meta-regressões dose-resposta atuais), versionado volume-engine@1 e snapshotado no plano pra reprodutibilidade do PDF. Save multi-call espelhando updateAssessment (apaga filhas e regrava; mapa clientKey→id reconecta os overrides aos exercícios recém-inseridos). PDF do treino estende o gerador @react-pdf (plaqueta AVALIX, helper barLayout puro em reports/charts.ts). database.types.ts estendido à mão e depois regenerado por gen types. v1.15: landmarks de volume (MEV/MAV/MRV por grupo) em src/features/workout/volume/landmarks.ts — diretriz prática de Renaissance Periodization/Israetel, NÃO constante validada, versionada landmarks-rp@1 e com ressalva exibida na UI e no PDF (mesma transparência do Siri/Brozek); mapeada à taxonomia de 20 grupos (Costas RP → lats/upper_back; ombro lateral/posterior → side/rear delts; grupos sem diretriz publicada exibidos só com o número). classifyVolume e landmarkBar puros e testados. Painel VolumeLandmarkPanel (reusado no builder e no detalhe) mostra cada grupo contra a faixa MAV e o teto MRV numa escala comum, colorido por zona (abaixo/efetivo/ótimo/alto/acima, cores via tokens da marca). PDF do treino idem (cor por zona + faixa + teto). Flag de anamnese no builder: se a última anamnese do aluno não está liberada ou tem flag_encaminhamento, banner âmbar com o nível de encaminhamento e link, antes de prescrever. Sem migration.

## Roadmap do módulo de treino (em andamento, nesta ordem)
Etapas A (v1.15) e B (v1.16) concluídas: A) landmarks de volume + flags de anamnese; B) ponte avaliação/postural → plano — selects de avaliação física e sessão postural de origem no builder (com tira de achados de composição corporal: %gordura+classificação ACE, massa magra/gorda, peso, puxados do snapshot da avaliação selecionada), card "Base da prescrição" no detalhe com links, e linha "Base" no PDF; sem migration (FKs source_assessment_id/source_posture_session_id já existiam, validadas por trigger de mesmo subject). Etapa C (v1.17) concluída: override completo por semana no builder (séries/reps/RIR/descanso/pular, com header e placeholders do valor do template; em branco = herda) — os campos já existiam no schema/save/PDF, faltava a UI; status do plano (rascunho/ativo/arquivado) via select no builder e ações rápidas no detalhe (setWorkoutPlanStatus faz update só da coluna, sem reescrever as filhas). Sem migration. Etapa D (v1.18) concluída: gestão da biblioteca de exercícios custom. updateCustomExercise (org_id congelado, RLS só deixa editar linha custom da org), página /exercicios (busca + filtro por grupo, criar/editar/excluir os custom; globais read-only com selo; excluir só owner/admin e o banco recusa via on delete restrict se o exercício estiver em uso). Form único ExerciseForm (create+edit, validado pelo zod do schema) reusado na biblioteca e no picker do builder — removeu a duplicação do CustomExerciseForm. Link em Configurações e no picker. Sem migration. Etapa E (v1.19) concluída: duplicar plano (botão no detalhe; clona a estrutura para o mesmo ou outro avaliado — efetivamente "usar como template entre alunos"; ao trocar de avaliado limpa as FKs de origem, que são validadas por mesmo subject; o novo plano nasce rascunho). duplicatePlanEditor é puro e testado e reusa planDetailToEditor/editorToSaveInput/snapshotFromEditor; useDuplicateWorkoutPlan invalida a lista do avaliado de destino. As chaves do editor (ids do banco do plano de origem) servem só como clientKey durante o save, que gera ids novos e remapeia os overrides. Sem migration; templates org-level nomeados ficam como evolução futura se necessário. Etapa F (v1.20) concluída: %1RM e teste de carga. Módulo puro src/features/workout/oneRm.ts (e1RM por Epley e Brzycki — estimativas, exibidas como tal; loadForPercent, repsForLoad inverso do Epley, percentTable 100..40, roundToIncrement 2,5 kg), testado com valores conhecidos. Componente OneRmCalculator (carga×reps+fórmula → 1RM estimado + tabela de %1RM com carga-alvo e reps por linha) reusado na página /ferramentas/1rm (link em Configurações) e embutido como colapsável no builder, sem sair da tela. Sem migration. Etapa G (v1.21) concluída: branding com o logo da org nos PDFs (treino e avaliação). Upload do logo em Configurações (owner/admin) para o bucket privado `logos` no path canônico {org_id}/logo.{ext} (a policy exige 1ª pasta = org_id + papel) e grava organizations.logo_path; preview com URL assinada. No PDF, loadOrgLogoDataUrl baixa via URL assinada e converte para data URL embutida (@react-pdf Image), com fallback na plaqueta AVALIX quando não há logo; o rodapé "Gerado pelo Avalix" permanece como atribuição. Sem migration (bucket logos e coluna logo_path já existiam). Etapa H (v1.22) concluída: compartilhar plano por WhatsApp. Módulo puro src/features/workout/share.ts (planShareText monta o resumo em texto com *negrito* do WhatsApp — divisões, exercícios, séries×reps, RIR; whatsappUrl só usa o número quando parece E.164 completo, senão abre sem destinatário), testado. No detalhe, botão Compartilhar abre painel com o texto pronto, "Enviar no WhatsApp" (wa.me), "Copiar texto" e, onde o navegador suporta arquivos no Web Share API, "Compartilhar PDF" (navigator.share com o File do PDF; fallback baixa). handlePdf refatorado num buildPdfBlob reusado por baixar e compartilhar. Sem migration. Etapa I (v1.23) concluída: fecha o ciclo prescrever→executar→medir. Migrations 0009 (schema) / 0010 (RLS) — FALTA APLICAR no Supabase + gen types. workout_logs (sessão executada, herda org_id+subject_id do plano por trigger b1 org_subject_from_plan; day_label é TEXTO snapshot da divisão, sobrevive à edição do plano; freeze; auditoria) + workout_log_sets (séries reais carga×reps×RIR; exercise_id estável do catálogo p/ progressão, não o workout_exercise que some ao reeditar; b1 herda org, b2 reusa check_exercise_scope, freeze; unique log+exercício+set_number). Validadas executando 0006→0010 num Postgres descartável (b1 resolve org/subject, CHECK rejeita set_number 0). Sem gate de consentimento (execução é dado operacional, igual ao plano). Módulo puro progress.ts (adesão = sessões feitas/previstas=semanas×divisões; exerciseProgression agrupa o histórico por exercício e guarda o melhor e1RM por dia, reusando estimateOneRm), testado. Página /avaliados/:id/treinos/:planId/execucao: card de adesão, form de registrar treino (escolhe divisão, prefill das séries do plano, carga×reps×RIR por série), histórico de sessões com exclusão, e progressão de carga (e1RM por exercício com sparkline via linePath). Botão Execução no detalhe. database.types.ts estendido à mão (regenerar com gen types após aplicar). Etapa J (v1.24) concluída: motor de SUGESTÃO de progressão (v2) — puro, versionado (progression-engine@1) e transparente, NÃO altera o plano sozinho (o treinador é o cérebro). Método: dupla progressão (progride reps dentro da faixa até o topo, aí sobe a carga) com autorregulação por RIR; suggestProgression devolve kind + carga/reps sugeridas + motivo. parseRepRange lê "8-12"/"10"; suggestDeload reduz ~60%; latestBestByExercise pega a melhor série (e1RM) da sessão mais recente como entrada. Tudo testado. Na página de execução, cada exercício do form de registrar mostra "última X×Y → sugestão Z kg × N · subir carga/+1 rep/reduzir" (com o motivo no title), não-destrutivo. listPlanSetHistory passou a trazer o rir. Sem migration. Etapa K (v1.25) concluída: agenda de sessões de avaliação. Migrations 0011 (schema) / 0012 (RLS) — FALTA APLICAR no Supabase + gen types. Tabela appointments (compromisso de avaliação com um avaliado; mesmo padrão das filhas — org herdado por trigger b1 org_from_subject, evaluator validado, freeze, auditoria, RLS via can_view_subject; sem gate de consentimento, agendar é ato administrativo), validada em Postgres descartável. Integração com Google Agenda/Apple/Outlook SEM OAuth e sem guardar tokens (decisão de segurança, AI_WORKFLOW §7/§20): módulo puro calendar.ts gera o link "Adicionar ao Google Agenda" (calendar.google.com/render?action=TEMPLATE) e o conteúdo .ics (universal), testado. Página /agenda (no nav): form de novo agendamento, lista de próximos + anteriores, cada um com link do Google e download .ics e exclusão; botão "Agendar" no detalhe do avaliado (preseleciona via ?subject=). database.types.ts estendido à mão (regenerar com gen types após aplicar).

## Roadmap do módulo de treino — CONCLUÍDO (A–K)
Todas as 11 etapas entregues e validadas (tsc/eslint/vitest/build): A) landmarks de volume; B) ponte avaliação→prescrição; C) override completo + status; D) gestão da biblioteca custom; E) duplicar plano; F) %1RM/teste de carga; G) branding no PDF; H) WhatsApp; I) log de execução (adesão + e1RM); J) motor de sugestão de progressão; K) agenda. Migrations a aplicar: 0009/0010 (Etapa I) e 0011/0012 (Etapa K).

## Melhorias pós-roadmap
Etapa L (v1.26) concluída: sequência semanal do plano — permite repetir uma divisão na semana (ex.: ABA). Migration 0013 adiciona workout_plans.weekly_schedule text[] (default '{}' = cada divisão uma vez; ATENÇÃO: o save passou a enviar essa coluna, então APLICAR 0013 ANTES de subir o código, senão salvar plano quebra). O motor de volume expande as sessões pela sequência (divisão repetida conta o volume 2x) e a adesão usa o total de sessões/semana, não de divisões. Builder tem editor de "Sequência semanal" (selects de divisão por sessão, adicionar/remover); detalhe e PDF mostram a sequência. Validada em Postgres descartável. Etapa M (v1.27) concluída: acentuação. Os 275 nomes de exercícios do seed estavam sem acento (ç, ~, agudos); corrigidos 92 nomes (Tríceps, Bíceps, máquina, Flexão de braços, Adução, Abdução, suspensão, sumô, glúteo, pélvica, pescoço, francês, búlgaro, triângulo, trenó, etc.) APENAS no campo do nome — as chaves de músculo ('biceps'/'triceps') seguem sem acento de propósito. Seed 0008 corrigido (fresh installs) + migration 0014 com 92 UPDATE (org_id null, casando pelo nome antigo, sem reinserir — preserva os ids referenciados). Validado em Postgres descartável: single-encoded (26 chars/27 bytes em "Agachamento sumô"). UI/labels já estavam acentuados (varredura confirmou). ATENÇÃO ao aplicar 0014: tem acento, então colar via VS Code (copiar/colar), não `type | clip` (o console cp1252 do Windows pode corromper UTF-8). Etapa N (v1.28) concluída: reordenar divisões e exercícios no builder. Drag nativo HTML5 (sem dependência nova) com pega (GripVertical), feedback de borda tracejada no alvo, + setas ↑↓ como fallback (touch/precisão). A ordem do array já vira o `position` no save (helper puro arrayMove), então nada de schema/migration. Exercício só reordena dentro da mesma divisão. Etapa O (v1.29) concluída: inteligência avaliação→prescrição (o fosso). Módulo puro e versionado contraindications.ts (contra-rules@1), testado, que NÃO é diagnóstico — é sinal pra o profissional REVISAR (o treinador decide). Cruza a anamnese estruturada (dor_queixas por região com intensidade≥3 ou lesão prévia; red_flags de coluna; alteracao_postural_diagnosticada; gestante) com cada exercício do plano (músculo primário/secundário + padrão de movimento): exerciseCautions devolve os motivos ("queixa em joelho direito (dor 5/10)", "sinais de alerta de coluna — evitar carga axial"); posturalEmphasis sugere ênfase (hipercifose→dorsais/rear delts; hiperlordose→abdômen/glúteos; escoliose→simétrico; gestante→adaptar). No builder: banner "Atenção pela anamnese" com a contagem de exercícios a revisar + as notas posturais, e um ⚠ âmbar (com motivo no title) ao lado de cada exercício sinalizado. Usa a anamnese mais recente do aluno; não bloqueia nada. Sem migration. Etapa P (v1.30) concluída: lembretes in-app (sem push nativo — custaria infra; anotado pra v2 com Web Push). Util puro src/lib/reminders.ts (relativeDayLabel Hoje/Amanhã/em N dias; daysSince; dueForReassessment com REASSESS_DAYS=90), testado. listLastAssessmentBySubject (última avaliação por aluno da org). Dashboard ganhou seção "Lembretes": próximas sessões dos próximos 7 dias (com rótulo relativo, link pra Agenda) e "Para reavaliar" (alunos ativos sem avaliação há 90+ dias ou nunca, com link). Agenda mostra rótulo relativo por compromisso (Hoje/Amanhã destacados). Sem migration. Etapa Q (v1.31) concluída: visão de carteira (/carteira, no nav). Página que cruza, por aluno ativo: status da última avaliação (gatilho de reavaliação), plano ativo, aderência (sessões feitas vs previstas=semanas×sessões/semana) e "sumido" (plano ativo sem treino registrado há QUIET_DAYS=10+). buildCarteira é puro e testado, ordena por urgência (reavaliar+sumido+baixa adesão). Queries org-wide novas: listOrgActivePlans (com embed workout_days(count) pra sessões/semana) e listOrgWorkoutLogSummary (count + última data por plano), ambas sob RLS. Header com contagens (ativos / reavaliar / sumidos); barra de adesão por aluno. Sem migration. Etapas R/S/T concluídas: R (v1.33) PDF como vitrine — tema visual compartilhado src/features/reports/pdfTheme.tsx (cabeçalho tipo papel-timbrado com faixa da marca + filete duplo, cartão de informações em grade, títulos de seção com filete, rodapé fixo com numeração de página), reusado pelos PDFs de avaliação e treino, removendo a duplicação de estilo/cabeçalho/rodapé entre os dois. T (v1.34) demonstração dos exercícios por LINK de busca de vídeo (decisão "só se gratuito": sem hospedar mídia nem API paga) — helper puro src/features/workout/demo.ts (exerciseDemoUrl monta busca no YouTube pelo nome) e componente ExerciseDemoLink reusado na biblioteca, no picker do builder e no detalhe do plano; zero storage/licença, o profissional escolhe a melhor demo e pode mandar pro aluno. S (v1.35) fundação/qualidade — controlClass (classe base dos selects/controles) estava duplicada verbatim em 14 telas, extraída pra src/lib/ui.ts; onde a largura difere do w-full padrão, compõe com cn(controlClass,'w-auto'); 0 vulnerabilidades em produção (npm audit --omit=dev). Além disso (v1.32): texto de consentimento LGPD revisado e aprovado, sobe de 0.2-rascunho para 1.0 (aceites antigos preservam a versão/hash do texto que a pessoa leu). NÃO fazer app do aluno.

## Próximo passo
ATUAL (v2.0, jul/2026): FEITO pelo Luiz — 0019 aplicada em produção, types regenerados (gen types confirma save_assessment/create_workout_log/client_errors no schema real), TOTP ligado e 2FA testado ponta a ponta em produção (Authy). A 0019 tinha sido validada antes rodando 0001→0019 + smoke funcional (TTL, trava de menor, caps, atomicidade dos dois saves) num Postgres 18 local descartável com stub auth/storage. RESTA: 1) acompanhar o primeiro acesso pós-deploy da v2.0 — o PWA voltou (selfDestroying removido + PwaUpdatePrompt montado); conferir se o aviso "Nova versão disponível" aparece nas instalações antigas e se o app instala/atualiza normal. 2) Opcional: `node scripts/find-orphan-photos.mjs` (com SUPABASE_SERVICE_ROLE_KEY no ambiente) pra confirmar que não há foto órfã pré-existente no bucket. 3) Beta com a avaliadora real. Histórico anterior abaixo.

JÁ FEITO pelo Luiz (jun/2026): todas as migrations até 0014 aplicadas no Supabase (inclui 0003/MFA, 0005, 0009-0013); secrets dos workflows configurados; revisão jurídica do consentimento concluída (texto aprovado, agora versão 1.0). PENDENTE pré-beta: habilitar TOTP em Authentication > Sign In/Providers (ou Multi-Factor Auth) no painel do Supabase — é só ligar o toggle, o código de 2FA do app já está pronto — e então testar o fluxo de 2FA ponta a ponta. Teste no celular real (iOS, captura de foto + instalação do PWA) ADIADO: o Luiz não tem iPhone; validar quando houver um aparelho. Revisão de segurança pré-beta concluída (jun/2026): RLS/consentimento/Storage/MFA sólidos, sem falha crítica nem alta. Gerada a migration 0015 (endurecimento, PENDENTE de aplicar): bloqueia des-revogação de consentimento (revoked_at via única null→timestamp; reconsentir cria novo registro), cap anti-flood de orgs por usuário no create_organization, e cast seguro do org_id no path do bucket logos (uuid_or_null evita erro com path malformado). Achado operacional aberto: testar enrollment + recuperação do 2FA antes de depender dele (perder o autenticador exige service role). database.types.ts regenerado do schema aplicado (sem divergência; build verde). Atualização do PWA sem F12 (v1.37): o problema era o service worker servir o shell cacheado pra abas/instalações abertas (autoUpdate não checava deploy sozinho). Trocado pra registerType 'prompt' + injectRegister null; registro manual em src/features/pwa/PwaUpdatePrompt.tsx, que checa update ao focar a aba/reconectar/a cada 1h e, ao achar versão nova, mostra um aviso discreto "Atualizar" (um toque recarrega). NÃO recarrega sozinho de propósito — é app de coleta, reload no meio de um formulário perderia dado. vite-env.d.ts ganhou a referência de tipos do vite-plugin-pwa/client. PDF de avaliação enriquecido (v1.38): além da rosca de composição, a seção "Evolução ao longo das avaliações" agora traz um minigráfico por métrica — % de gordura, peso, IMC, massa magra e massa gorda — cada um só aparece com ≥2 pontos (peso/IMC existem sempre; %gordura/massas só com protocolo de composição). AssessmentPdfData.history virou AssessmentHistoryPoint (date + weightKg/bmi/bodyFatPct/leanMassKg/fatMassKg); o call site (AvaliacaoDetalhe) monta a série usando computeBmi e o snapshot de resultados. Reusa linePath de reports/charts. Bom pra entregar ao aluno. v1.39: o PDF ganhou também a "Evolução das circunferências (cm)" — um minigráfico por perímetro mais medido (helper puro buildCircSeries no PDF espelha o buildCircData da tela /evolucao: sites com ≥2 medidas, ordenados por nº de medidas, top 6, alinhados às datas). AvaliacaoDetalhe busca listSubjectCircumferences na geração e passa em circumferenceHistory; reusa MiniTrend e circumferenceLabel. v1.40: gráficos do PDF refeitos (o MiniTrend era pobre, sem escala nem valores). Agora TrendChart desenha um cartão de verdade por métrica: linha com PONTO marcado em cada avaliação, VALOR inicial e final escritos sobre os pontos (de-quanto→pra-quanto), Δ (variação) no cabeçalho na cor da métrica, linhas de referência mín/máx com a escala no eixo, e datas nas pontas. PDF limitado aos ÚLTIMOS 10 pontos (history.slice(-10) e janela de 10 datas no buildCircSeries); a tela /evolucao do app segue mostrando todos. Texto SVG no @react-pdf vai via prop style (não props diretas — props diretas não compilam). Atenção: nada de glifos fora do WinAnsi nos rótulos (sem '→'); usar sinal +/- pro Δ. Teste de fumaça assessmentPdf.render.test.tsx (renderiza o PDF em node via generateAssessmentPdf e exige bytes) cobre Line/Polyline/Circle/Text em runtime. v1.41 (ajuste de feedback sobre o PDF real): escala dos gráficos maior e legível (3 referências máx/meio/mín com a escala à esquerda, fontSize 7, cor AXIS_COLOR #5b5570 em vez do muted apagado, GRID_COLOR mais visível); valor inicial à direita do 1º ponto e final em destaque (bold, maior) à esquerda do último; gráficos maiores (CHART_H 104). Cabeçalho: removido o filete fino violeta, ficou só o grosso da marca (pdfTheme.ruleThin eliminado). InfoCard refeito: nome do avaliado em destaque no topo + filete + grade rótulo/valor (infoHead/infoGrid/infoCell), em vez dos pares soltos. Limitação: não dá pra rasterizar PDF no ambiente (sem poppler) — ajuste fino visual é por inspeção do Luiz. Iteração seguinte: o valor ATUAL (último ponto) estava caindo em cima da linha; movido pra FORA do plot, ao lado do último ponto (PX1 reduzido pra 212 reservando margem direita; o valor inicial virou rótulo pequeno acima do 1º ponto, só referência). Mais dois ajustes: cada cartão de gráfico agora é indivisível (wrap={false} no trendCard) pra não partir título numa página e gráfico na outra; e o rodapé deixou de mostrar o semver do motor ("motor de cálculo 1.0.0" enfeiava) — passou a "Calculado pelo motor Avalix" (avaliação) e "Montado no Avalix" (treino). O engine_version continua no snapshot pra reprodutibilidade, só não é impresso. v1.43: evolução das circunferências no PDF cobre membros também. Antes pegava as 6 "mais medidas" (empate deixava só o tronco); agora buildCircSeries (exportado e testado) usa uma lista de PRIORIDADE por região — tronco central (cintura/abdômen/quadril) → membros inferiores (coxa proximal/medial/distal, panturrilha) → superiores (braço contraído/relaxado, antebraço) → tórax/pescoço/ombro — com o bilateral D/E entrando como MÉDIA dos lados medidos (um gráfico por região, não dois). Cap 12 gráficos; sites customizados fora do catálogo entram no fim por nº de medidas. Só aparece região com ≥2 pontos. v1.44: contagem de volume — modo REFINADO opcional. O PADRÃO segue 'fractional' (primário 1,0 / secundário 0,5 fixo), que é o de melhor evidência nas meta-regressões dose-resposta e o que é PERSISTIDO no plano e usado no PDF. O refinado (VolumeMethod 'refined' no motor) mantém o secundário em 0,5 nos exercícios COMPOSTOS e reduz pra 0,25 nos ISOLADOS (regra por movement_pattern==='isolation', transparente, cobre 100% incl. customizados — NÃO números por exercício). É CONVENÇÃO prática, não constante validada (nenhuma literatura dá frações por exercício; EMG não prediz hipertrofia — Vigotsky 2022). Exposto só como TOGGLE no VolumeLandmarkPanel (default Padrão), com explicação (VOLUME_METHOD_NOTE_REFINED) e referências clicáveis (VOLUME_METHOD_REFS); recomputado ao vivo no builder e no detalhe, NÃO altera o snapshot salvo nem o PDF. secondaryWeight/countWeekVolume/buildVolumeSnapshot ganharam o param method (default fractional); ExerciseMeta e VolumeExercise ganharam movementPattern (opcional). Puro e testado. Decisão de produto ainda aberta: a classificação de % de gordura usa a tabela ACE (geral por sexo); avaliar trocar por tabela etária (ACSM/Pollock) com fonte verificada. Opcional: resumo IA (identificado/anônimo). Observações: ETAPA3.md e ETAPA3-1.md NÃO são duplicados (592 vs 43 linhas, conteúdos distintos) — a sugestão do V1.1 de apagar um estava equivocada; nada foi removido. vite-plugin-pwa traz 6 vulnerabilidades só em devDependencies (build-time), 0 em produção.

v1.45 (revisão geral + correções): auditoria completa do código (lint/181→193 testes/build verdes). 1) BUG DE PRECACHE DO PWA corrigido: desde a v1.33 o @react-pdf mora no chunk pdfTheme-*.js (1,46 MB) e o globIgnores só excluía assessmentPdf-*.js — o PDF inteiro entrava no precache do service worker (instalação/atualização 1,46 MB mais pesada); agora ignora pdfTheme/assessmentPdf/workoutPdf (precache caiu ~2.766→~1.300 KB). 2) SAVES ATÔMICOS — migration 0016 (ATENÇÃO: APLICAR ANTES DE SUBIR O CÓDIGO, igual à 0013): RPCs security INVOKER (RLS do chamador continua valendo) replace_assessment_readings e replace_workout_plan_children fazem o delete+reinsert das filhas numa transação só; o caso "consentimento revogado entre criar e editar" (delete passava, reinsert falhava, leituras sumiam) agora reverte inteiro — validado em Postgres descartável com stub auth/storage, incluindo o teste do rollback. createWorkoutPlan apaga o plano (best-effort) se a estrutura falhar, pra não deixar plano vazio. A 0016 também cria as views security_invoker last_assessment_by_subject (distinct on) e workout_log_summary (count+max por plano): Dashboard/Carteira deixam de baixar todas as avaliações/logs da org. database.types.ts estendido à mão (Functions + Views; regenerar com gen types depois de aplicar). 3) Editores com key por rota: TreinoNovo (`<Builder key={planId}>`) e AvaliacaoNova (`<Form key={assessmentId}>`) remontam ao trocar de plano/avaliação (useState(initial) não acompanha props); AvaliadoForm reseta pro form vazio ao voltar pro modo novo. 4) OrganizationProvider ordena membership por created_at (com 2+ orgs, qual carregava era loteria; V1 continua 1 org). 5) buildVolumeSnapshot grava os weights REALMENTE usados (refined = 0,5/0,25 com isolationSecondary no tipo; antes o snapshot diria 0,5 fixo mesmo no refinado). 6) normalizeDbError em lib/errors.ts (testado): RLS/unique/FK/rede/JWT expirado em pt-BR; aplicado nos ~15 pontos que mostravam (e as Error).message cru; exceções pt-BR dos nossos triggers passam intactas. 7) ConfirmDialog (componente novo, `<dialog>` nativo com showModal, testado) substituiu os 9 window.confirm (Agenda, Execução, AvaliacaoDetalhe, TreinoDetalhe, ExerciciosBiblioteca, Configurações/2FA, PosturaSessaoDetalhe foto+sessão); a exclusão definitiva do avaliado continua com confirmação por nome digitado. 8) Dependências: imports migrados react-router-dom→react-router (v7: o -dom é só re-export) e removidos react-router-dom e @radix-ui/react-slot (o código usa o pacote unificado radix-ui); package.json version 1.45.0 (antes 0.1.0). 9) Infra de teste de componente: jsdom + @testing-library/react (devDeps), pragma @vitest-environment jsdom por arquivo (os testes puros seguem em node); primeiros testes de componente: OneRmCalculator e ConfirmDialog. 10) Acentuação nas strings visíveis: VOLUME_METHOD_NOTE/NOTE_REFINED/REFS (engine.ts) e VOLUME_LANDMARKS_NOTE (landmarks.ts) acentuados (aparecem na UI e no PDF; WinAnsi aceita acento latino, o proibido é glifo tipo '→'); varredura automatizada sobre strings/JSX não achou mais nada — rotas/nomes de arquivo (avaliacoes-*.csv) ficam ASCII de propósito, e o texto de consentimento 1.0 NÃO foi tocado (hash jurídico). Comentários de código sem acento ficaram como estão (convenção mista, sem impacto no usuário).

v1.46 (anamnese respondida pelo aluno via link): spec em docs/anamnese_link_aluno_spec.md. O personal gera um link, manda por WhatsApp/e-mail, e o aluno responde SEM login; a resposta entra como pendente e só vira anamnese oficial no aceite do personal. Migration 0017 (APLICADA + types regerados antes de subir o código, igual à 0013/0016): tabela anamnese_intakes no mesmo padrão das filhas (org por trigger org_from_subject, freeze de org/subject/token_hash/created_by, updated_at, auditoria — audit_logs.user_id é nullable, o envio anônimo grava ator nulo); RPCs get_anamnese_intake/submit_anamnese_intake (security definer, grant a anon, hash do token em SQL com sha256() NATIVO do Postgres = mesmo hex do sha256Hex do front, server-side de propósito) e accept_anamnese_intake (security invoker, transação consent_records+anamneses com o gate vindo do TS por parâmetro; consent ANTES da anamnese porque anamneses_insert exige has_active_consent); view security_invoker pending_anamnese_intakes pra alimentar badge/dashboard numa query. RLS só authenticated derivada do subject; anon nunca toca a tabela direto (só pelas RPCs). Divulgação mínima (D5): o link devolve só marca da org + primeiro nome + sexo (necessário pra decidir a seção B6), nada de telefone/e-mail/observações/nascimento nem dado de outro avaliado. Front: página pública /a/:token FORA do RouteGuard (isIntakePath libera; não depende de sessão/org e mostra o NOME da org em texto, não o logo — que está em bucket privado sem acesso anônimo); o formulário, antes preso em AnamneseNova, foi extraído pra src/features/anamnesis/AnamneseForm.tsx (AnamneseCamadaA/B + GateBox) e o resumo read-only pra AnamneseResumo.tsx, agora reusados por AnamneseNova/AnamneseDetalhe/pública/revisão (removeu ~400 linhas duplicadas). Camada de dados intake.ts/intakeHooks.ts: token base64url de 32 bytes gerado no cliente, só o hash gravado, link válido 7 dias e de uso único (o cru só aparece uma vez na geração). Pendência bem VISÍVEL em 3 lugares pra não esquecer: contador vermelho no item Início do AppShell, card de alerta no Dashboard ("Anamneses aguardando revisão" com link por aluno) e selo "Aguardando revisão" + botão Revisar no perfil do aluno; a tela /avaliados/:id/anamnese/intake/:intakeId mostra consentimento + resumo + gate e Aceitar/Recusar (aceitar chama a RPC atômica e navega pra anamnese criada). Gerar link no perfil com Copiar/WhatsApp; consentimento LGPD dado pelo próprio titular no envio (nome/versão/hash/timestamp — mais forte que hoje). lint/build/192 testes verdes (sem teste novo: gate/sha256 reusados já são testados). Fora de escopo (fica pra depois): notificação por e-mail/WhatsApp ao personal quando o aluno responde (exige gatilho server-side). NÃO fazer app do aluno — a página pública é single-shot, sem conta.

v2.0 (jul/2026, auditoria técnica completa + correções + features): auditoria formal do código inteiro (migrations, RLS, front, PWA) gerou 12 achados e 4 propostas implementadas; lint/226 testes/build verdes. CORREÇÕES: 1) PWA REATIVADO — o selfDestroying do hotfix de reset saiu do vite.config e o PwaUpdatePrompt (que estava definido mas NUNCA montado) entrou no main.tsx; quem não abriu o app durante a janela do reset pega o SW novo direto (sw.js é no-cache no Cloudflare). 2) EXCLUSÃO DEFINITIVA À PROVA DE ÓRFÃOS (LGPD) — o remove() do supabase-js não reporta falha por objeto e a policy do Storage resolve o objeto PELA LINHA: linha morta + arquivo vivo = foto órfã inapagável pelo app. Agora lib/storage.ts (a costura única prevista pra migração R2) remove e VERIFICA por list() antes de apagar qualquer linha (deletePhoto/deleteSession/deleteSubjectCompletely); sobrou arquivo → erro e nada é excluído do banco. scripts/find-orphan-photos.mjs (service role) varre órfãos pré-existentes (--delete remove). 3) MIGRATION 0019 (aplicar ANTES de subir a v2.0): accept_anamnese_intake ganhou select...for update (dois aceites simultâneos duplicavam subject/consent/anamnese no cadastro); submit_anamnese_intake ganhou trava de MENOR também no fluxo de anamnese pura (lia só o registration; agora checa o birth_date do subject — LGPD art. 14) e caps de tamanho nos jsonb (100/20 KB); get_anamnese_intake NÃO devolve mais o logo_path (vazava o uuid da org pro anônimo; o front nem usava — retorno mudou, drop+create); check de TTL máximo de 30 dias em expires_at (o desenho do token mora no banco, não no cliente); RPCs novas security invoker save_assessment (header+leituras numa transação — antes o snapshot novo podia ficar com leituras velhas se a 2ª chamada falhasse) e create_workout_log (log+séries — antes séries falhando deixavam sessão vazia contando na adesão), ambas com args anuláveis via default null (gen types futuros ficam compatíveis); tabela client_errors (observabilidade mínima sem serviço externo: o front grava erros de runtime — ErrorBoundary + window.onerror/unhandledrejection em lib/errlog.ts, com throttle/dedup e org via setter do OrganizationProvider — e owner/admin lê em /auditoria). 4) parseAnswers ESTRITO (fronteira anônima): o payload do intake vem do request do aluno; agora cada campo é validado por tipo com zod + .catch (valor de tipo errado cai no default vazio, nunca lança) — red_flags como string não confunde mais o gate, cirurgias malformadas não quebram a revisão; testes de payload hostil. 5) daysSince parseia data-só como dia LOCAL (new Date('aaaa-mm-dd') é UTC e deslocava lembretes/reavaliação em 1 dia em UTC-3). 6) AvaliadoForm usa normalizeDbError (último erro cru remanescente). 7) A11y: aria-label nos trios de dobras, botões-ícone e campos de override. 8) TreinoNovo desidratado 1194→791 linhas: AnamneseFlag/SourceCard/ExercisePicker/WeeksCard extraídos pra features/workout (sem mudança de comportamento; o X de fechar o picker deixou de ser ícone de lixeira). 9) TESTES DE FLUXO CRÍTICO (jsdom, hooks mockados): AnamneseRevisar (aceite revalida com zod e chama a RPC com o subject convertido; recusa em 2 etapas; intake aceito não reexibe botões) e DangerZone (exclusão só com nome exato). FEATURES: P1 COMPARAÇÃO — /avaliados/:id/comparar (links no perfil e na evolução), tabela antes→depois com Δ colorido só onde há direção clara (%G/massas; peso/IMC/perímetros neutros — o objetivo varia), motor puro buildComparison testado (união de perímetros na ordem do catálogo, customizados no fim). P4 RASCUNHO LOCAL — lib/draft.ts (localStorage, TTL 24h, debounce 600ms, purge automático, testado) + useFormDraft nos 3 formulários longos: AvaliacaoNova (só no criar), AnamneseNova e a página pública do aluno (chave usa prefixo do token; limpa no envio e para de salvar após sucesso; aceite do termo NÃO é restaurado). P5 AUDITORIA VISÍVEL — página /auditoria (card em Configurações, owner/admin): trilha paginada (50/pág, count exato, nomes via profiles) com rótulos pt-BR por ação/tabela, + card de erros do app (client_errors, limpar com ConfirmDialog). P6 PDF DE EVOLUÇÃO — generateEvolutionPdf no MESMO chunk do assessmentPdf (reusa TrendChart/pdfTheme): resumo do período (início/atual/variação por métrica, helper puro evolutionSummaryRows) + cartões de tendência + circunferências; botão na tela /evolucao, logado como PDF_REPORT. P7 DETEÇÃO POSTURAL ASSISTIDA — botão "Detectar pontos" no editor de anotações: MediaPipe Pose (tasks-vision 0.10.35, Apache-2.0, wasm do jsdelivr PINADO na versão + modelo lite float16 ~5,5 MB do CDN do MediaPipe — custo zero, tudo roda NO APARELHO, a foto não sai do navegador) SUGERE pontos (orelhas/joelhos/tornozelos) e linhas (ombros/quadris) que caem direto no espaço 0..1 do AnnotationCanvas; módulo puro pose.ts (visibility≥0.5, clamp, testado) separado do carregador poseDetect.ts (chunk lazy, fora do precache via globIgnores); disclaimer explícito: sugestão, não diagnóstico — o profissional ajusta/apaga. 2FA: scripts/test-2fa.mjs testa ponta a ponta com TOTP calculado localmente (RFC 6238 em node:crypto, sem dependência) e desfaz o enrollment ao final; se o toggle do painel estiver desligado, falha no passo 2 com mensagem clara. Rate limiting das RPCs anônimas: mitigado no banco pelos caps de tamanho + token de 256 bits; limites de requisição são config do painel (Auth rate limits) — anotado, sem código. Decisão de produto AINDA aberta: classificação de %G por tabela etária (ACSM/Pollock) segue esperando fonte verificada (regra do projeto: nenhuma tabela sem exemplo publicado batendo). NÃO fazer app do aluno.

v2.1 (link de convite sobrevive a reload): feedback de campo — o personal recarregava a página depois de gerar o convite e o link "sumia" (por desenho: o banco guarda só o HASH do token desde a 0017, então após o reload nem o servidor conhece a URL). Solução sem furar o desenho: a URL fica salva no localStorage DO APARELHO QUE GEROU (src/features/anamnesis/linkStore.ts, testado — expira junto com o convite, some no cancelamento, purge a cada listagem), e as listas de convites vivos (tela Avaliados e seção de anamnese do perfil) agora mostram "gerado dd/mm hh:mm · expira dd/mm/aaaa" com botões Copiar/WhatsApp reexibidos via IntakeLinkButtons enquanto o convite estiver pendente. Em OUTRO aparelho o segredo nunca existiu — a lista mostra a dica "copie no aparelho onde foi gerado" e o caminho é cancelar e gerar novo. Textos dos cards atualizados (antes diziam "o link não é exibido de novo"). Sem migration.

v1.47 (cadastro feito pelo próprio aluno via link): extensão da v1.46 — além do link de anamnese amarrado a um avaliado existente, o personal gera um LINK DE CADASTRO (botão "Convidar por link" na tela Avaliados, com Copiar/WhatsApp): o aluno preenche os próprios dados (nome, nascimento, sexo, altura/telefone/e-mail opcionais, responsável legal se menor) E a anamnese numa página só, aceita o termo e envia. NADA vira registro oficial no envio: o cadastro fica no jsonb `registration` do intake e o avaliado (subjects) só é criado no ACEITE do personal, na MESMA transação que cria consent_records + anamnese (mesmo checkpoint humano D1 da 0017; o fluxo antigo de cadastro manual + link por avaliado continua intacto). Migration 0018 (APLICAR ANTES de subir o código, igual 0013/0016/0017 — RPCs mudaram de assinatura/retorno): anamnese_intakes ganha kind ('anamnese'|'cadastro_anamnese', congelado), registration jsonb, resulting_subject_id, e subject_id vira NULLABLE com check ((kind='anamnese') = (subject_id is not null)); trigger b1 novo app.intake_org (com subject copia a org dele; sem subject exige org_id, validado pela policy); RLS das 4 policies vira CASE — com subject segue can_view_subject_id, sem subject é is_member+mfa_satisfied (org-wide; V1 solo, documentado); get_anamnese_intake devolve kind (left join, nome/sexo nulos no cadastro); submit_anamnese_intake ganha p_registration (obrigatório no cadastro, proibido na anamnese pura; valida nome/nascimento/sexo server-side + trava menor de idade → signer responsavel; select for update contra envio duplo); accept_anamnese_intake ganha p_subject jsonb e devolve table(subject_id, anamnese_id) — no cadastro insere subjects primeiro (security invoker: policy is_member+mfa e trigger check_evaluator valem; quem aceita vira o avaliador responsável), depois consent + anamnese; view pending_anamnese_intakes com left join + kind + nome via coalesce(subjects, registration). Front: página pública detecta kind e mostra o bloco "Seus dados" (MESMO zod do cadastro manual — subjectFormSchema — via react-hook-form; sexo escolhido decide a seção B6; menor de 18 exige responsável e trava o signatário em responsável); tela de revisão (rota nova /avaliados/intake/:intakeId, fora do padrão /avaliados/:id porque não há avaliado ainda) mostra o cadastro enviado, avisa DUPLICATA por nome igual, REVALIDA o registration com o mesmo zod e converte com formToInsert antes de aceitar ("Aceitar e cadastrar" navega pro avaliado novo; qualquer campo é ajustável depois na edição normal); convites vivos listados na tela Avaliados (aguardando resposta/expira + cancelar; aguardando revisão + Revisar) e pendências entram no badge do AppShell e no card do Dashboard ("novo cadastro"). database.types.ts estendido à mão (regenerar com gen types depois de aplicar a 0018). lint/build/192 testes verdes. Nota de deploy: entre aplicar a 0018 e publicar a v1.47, evitar ACEITAR intakes na versão antiga (o retorno da RPC mudou de uuid pra tabela; o aceite grava certo, só o redirect da tela antiga quebraria).
